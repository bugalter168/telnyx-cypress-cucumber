/**
 * Site under test.
 *
 * Import-free for the same reason as config/viewports.ts — see the note there.
 *
 * This is a public, third-party PRODUCTION site. Traffic must stay read-only: no account
 * creation, no form submissions, no payment flows, no load testing, no parallelism.
 */
export const baseUrl = 'https://telnyx.com';
